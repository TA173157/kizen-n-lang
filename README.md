# kizen-n-lang
A custom, statically-typed programming language implemented from scratch using LLVM. This project features a hand-written lexer and parser, a robust AST, and compiles directly to LLVM IR, enabling native machine code generation. Focuses on exploring compiler design principles, type safety, and low-level code generation.
